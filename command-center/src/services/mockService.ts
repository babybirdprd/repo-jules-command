// services/mockService.ts
// import { Job, JobType, AgentMode, PrDetails } from '../types';

export const MockService = {
  // All mock logic removed as we are connecting to real Tauri backend (even if backend mocks some parts)
  // This file is kept if we need pure frontend mocks later, but for now we clean up unused variables.
};
