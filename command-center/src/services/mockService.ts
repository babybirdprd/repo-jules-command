// services/mockService.ts
// import { Job, JobStatus, PrDetails } from '../types';

export const MockService = {
  // Mock implementations if needed, but we seem to be using TauriService now.
  // We'll keep this file for reference or if we need to switch back to mocks quickly.
};
